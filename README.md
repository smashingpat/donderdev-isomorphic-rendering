# Isomorphic Rendering for dummies

Project showing off how to render a page with React

### install

```sh
npm install
```

### usage

```sh
npm start           # starts the server
npm run dev         # starts the server in watch mode
```
